# Codex instructions for AdhdAgenda

Project root:
- /Users/brad/Documents/bradckennedy

General rules:
- Do not work on main directly. Create or use a feature branch.
- Keep changes tightly scoped to the task.
- Prefer editing existing files over creating unnecessary new ones.
- Do not redesign pages unless explicitly instructed.

Homepage hero instructions:
- The source of truth for the homepage hero is:
  - assets/landing/image.png
- When asked to update the homepage hero or landing page hero, match that image as literally as possible.
- Do not reinterpret the design.
- Do not simplify the layout.
- Do not change copy unless explicitly instructed.
- Do not preserve the old hero if it conflicts with the source image.

Homepage files:
- index.html
- assets/homepage.css
- assets/site.css only if necessary

Implementation expectations:
- Use real HTML/CSS, not a single flat image, unless explicitly requested.
- Keep the rest of the homepage unchanged unless minor adjustments are required to prevent breakage.
- Mobile-first responsive behaviour is required.

Before finishing:
- Run relevant tests if the task affects behaviour.
- Summarise exactly what changed.
